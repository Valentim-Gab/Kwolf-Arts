# Kwolf Arts
WebSite for selling digital arts.

```bash

Using {
  Database:
    - PostgreSQL;

  API:
    - Java + Maven (Servlet);
    
  WEB:
    - HTML + JSP + bootstrap;
    - CSS + bootstrap;
    - JavaScript.
}

```
